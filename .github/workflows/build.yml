name: Build Executables

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            output: xadrez
          - os: windows-latest
            output: xadrez.exe
          - os: macos-latest
            output: xadrez
    
    steps:
    - uses: actions/checkout@v6
    
    - name: Compile on ${{ matrix.os }}
      run: gcc -fPIE -fstack-protector-strong -D_FORTIFY_SOURCE=2 menu.c jogo.c jogadasvalidas.c traducao.c -o ${{ matrix.output }}
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v7
      with:
        name: xadrez-${{ matrix.os }}
        path: ${{ matrix.output }}